spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/medical_data_test
    driver-class-name: org.postgresql.Driver
    username: admin
    password: medical_pw
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true